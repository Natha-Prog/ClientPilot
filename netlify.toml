# Netlify: build only the Vite frontend (monorepo)
[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback for React Router (e.g. /dashboard, /clients)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
